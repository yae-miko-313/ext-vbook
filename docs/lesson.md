# Lesson

- Khi user yêu cầu bump version của một extension, phải bump cả version trong `extensions/<extension>/plugin.json` và entry tương ứng trong root `plugin.json`, sau đó build lại file `plugin.zip` của extension đó.
