// language.js — type: "from" (source only) | "to" (target only) | omitted (both)
function execute() {
    return Response.success([
        { id: "en", name: "English" },
        { id: "vi", name: "Vietnamese" }
    ]);
}
